/api/v1/cluster:
  get:
    tags:
      - Cluster
    description: "sample"
    produces:
      - application/json
    responses:
      200:
        description: "sample"
  post:
    tags:
      - Cluster
    description: "sample"
    produces:
      - application/json
    parameters:
      - in: "body"
        name: "body"
        description: "Cluster object that needs to be added"
        required: true
        schema:
          $ref: "#/definitions/_Cluster"
    responses:
      200:
        description: "sample"
/api/v1/cluster/{id}:
  get:
    tags:
      - Cluster
    description: "sample"
    produces:
      - application/json
    parameters:
      - name: id
        in: path
        type: integer
    responses:
      200:
        description: "sample"
  put:
    tags:
      - Cluster
    description: "sample"
    produces:
      - application/json
    parameters:
      - name: id
        in: path
        type: integer
      - in: "body"
        name: "body"
        description: "Cluster object that needs to be update"
        required: true
        schema:
          $ref: "#/definitions/_Cluster"
    responses:
      200:
        description: "sample"
  delete:
    tags:
      - Cluster
    description: "sample"
    produces:
      - application/json
    parameters:
      - name: id
        in: path
        type: integer
    responses:
      200:
        description: "sample"
